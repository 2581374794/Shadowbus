# P2P 对战开发原则

本项目的 P2P 对战以 **Host 充当原版权威服务器** 为目标，而不是两端各自推演后再用自定义快照修补状态。

## 必须遵守

## 不可违反的架构硬约束

以下规则优先于本项目中任何旧实现、兼容代码、临时修复和阶段勾选状态：

1. **彻底放弃旧 P2P 架构**：不得使用双方各自推演后再用快照、Manifest、authority replay、动作后替换或单卡特判修补状态的架构。旧代码未删除前，不得宣称重构完成。
2. **客户端行为不得改写**：客户端必须继续使用游戏原版的出牌、攻击、进化、融合、回合切换、动画、请求生成和响应接收逻辑。P2P 代码只能替代网络传输和官方服务器，不得改变客户端的游戏语义或调用时序。
3. **Host 只模拟官方服务器**：Host 接收原版客户端请求，根据反编译得到的请求/响应格式推测并构建服务器响应；Host 是唯一权威状态来源，不得把 Guest 本地状态或自定义快照当作权威结果。
4. **响应必须回到原版接收链**：Host 生成的响应必须通过原版 `NetworkBattleReceiver`、`NetworkBattleData.BeforeSettingReceiveData`、`ReplaceReceivedCard`、`OperateReceive` 和 `NetworkOperationCollection` 执行。不得用 P2P 自定义执行器绕过这些入口。
5. **扩展字段最小化**：只有原版请求/响应完全无法承载的服务器状态才允许增加扩展字段；扩展只能补充服务器数据，不能驱动第二套客户端推演路径。
6. **验收标准**：编译、协议单元测试或代码注释不能证明架构完成。必须能说明原版客户端链路、Host 服务端转换、双方响应进入原版接收链，并经过实际双客户端验证；未验证部分必须明确标记为未完成。

1. 遇到 P2P 对战异常时，先在原版反编译源码 `D:\Modding\shadowverse\Assembly-CSharp` 中定位对应网络链路：客户端请求、服务器响应数据、`NetworkBattleReceiver` 解析、`NetworkBattleData.BeforeSettingReceiveData`、`ReplaceReceivedCard`、`OperateReceive.StartOperate` 以及后续原生操作。
2. 优先复用或模拟原版协议和函数调用顺序。Host 的职责是替代已关闭的官方服务器生成并下发原版客户端应接收的数据；不应以独立的自定义同步机制替代已存在的原版网络语义。
3. 只有在原版协议没有承载某项本地状态时，才能添加 P2P 扩展字段；扩展必须在原版接收/执行边界接入，不能绕过或改变原版操作顺序。不得用按单张卡、单个效果的特判掩盖通用网络问题。
4. 每次分析或修复 P2P 问题时，报告中必须明确说明：
   - 原版对应的网络处理逻辑和调用顺序；
   - 当前 Host 服务器实现与原版的具体偏差；
   - 修复如何恢复原版语义；
   - 仍无法由原版逻辑覆盖的扩展状态及其原因。
5. 朋友对战不需要反作弊；这不改变 Host 必须作为唯一权威状态来源、并按原版服务器语义驱动客户端的要求。
