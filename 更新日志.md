# Shadowbus 更新报告（2.5.1 → 2.5.6）

Shadowbus 是《影之诗》的离线 / 联机 Mod 工具。本文覆盖 2.5.1 之后到 2.5.6 的全部改动。

**归属：联机（P2P / 房间对战 / SocketIO 服务端）与本地卡牌语音由作者本人实现；其余为本次新增。**

「影响面」：**卡牌** = 做卡改卡相关，**界面** = 游戏里的列表与按钮，**联机** = 网络对战，**内部** = 只影响插件自身。

**2.5.6 的重点**：回放能用（离线录像 + 播放 + 剧情主战者）、特效可借（自制卡演出）、卡面取图不再猜错（手牌紫红 / 卡组编辑丢图）、路径与日志收进资源目录、卡片列表与卡组读取的健壮性（缺卡不再炸整条流程）、BossRush 与剧情离线数据补齐、一批界面与诊断工具。

---

## 一、本版做了什么（速览）

- **卡片**：新增「借特效」（6 个声明式字段）；卡面材质目录实测化 + field/spell 兜底；重印卡包名重定向；借来的资源组每次都补拉 + 卡池预先请求；材质/贴图保活；缺卡与坏牌组不再炸列表。
- **回放**：离线 AI 战自动录像；开局换牌两条 op 补齐；`replay_info.json` 的职业与主战者补正（含剧情章节表反查）；发牌空引用防护；回放列表弹窗新增「打开回放文件夹」按钮。
- **剧情**：官方主战者表 / 别名 / 可用性表；章节特殊战斗设置（`Mods/StorySpecialBattles/*.json`）；章节表情变体；词条文本回退；剧情文本按语言取包。
- **BossRush**：本地进度与存档、卡组与能力捕获、隐藏 Boss、主战者选择窗口、增益/礼包文本简繁双语、界面清理。
- **AI 与练习**：自定义练习入口、剧情 AI 选择对话框、官方 AI（牌组/风格/表情）离线数据、AI 兜底数据、AI 出牌看门狗。
- **离线数据与界面**：个人资料（`ProfileOfflineData`）、解谜（`PuzzleOfflineData`）、「其他」页调整与货币修改、结算界面「任务」按钮移除、语言表只留中文、主界面清理。
- **路径与日志**：`persistentDataPath` 与 `PlayerPrefs` 全量重定向到资源目录；引擎日志用启动器或目录联接归位；任务看门狗与 `PerfTrace` 定位卡死。
- **工具**：`PatchCheck.exe`、`个人数据清理.bat`、`游戏资源链接工具.bat`、随游戏分发的 Mod 教程、网页版卡牌编辑器。

---

## 二、版本总表

### 2.5.1 – 2.5.5（已有功能）

| 版本 | 功能 | 影响面 | 代码位置 | 归属 |
| --- | --- | --- | --- | --- |
| 2.5.1–2.5.5 | 联机对战 | 联机 | `P2P/*`、`Server/Room/*`、`Server/SocketIO/*` | **作者本人** |
| 2.5.2 | 卡名可点击词条 | 卡牌 | `RegisterCardNameKeywords`、`Master_StartLoadBattleKeyWordText_Postfix` | 新增 |
| 2.5.2 | 卡牌列表按费用排序 | 界面 | `CompareCardIdByCost`、`SortCardIdListByCost`、`SelectAllCardIDInConditionMask_Postfix`、`SortIDList_Postfix` | 新增 |
| 2.5.3 | 借用原版卡面 / 语音库 / 浮点字段 / 闪卡自动派生 | 卡牌 | `normalArtCardId`、`extraVoiceIds`、`floatFields`、`TryAddFoilCompanion` | 新增 |
| 2.5.4 | 跨阶段取图、卡面类型行、借图继承取景、来源包按需加载、衍生卡卡面修复 | 卡牌 | `InheritArtworkFraming`、`ResolveMissingArtwork`、`TryLoadArtSourceBundleOnDemand` | 新增 |
| 2.5.5 | 一卡一文件夹、`imageFiles`/`voiceFiles`、移除 `CardImages`/`CardVoices` | 卡牌 | `ModCardAssets.cs`、`PathHelper` | 新增 |
| 2.5.5 | 死神 / 牌库耗尽全模式修复 | 卡牌 | `BattleVfxGuards.cs` | 新增 |
| 2.5.5 | 本地音频自解码 + 原生输出 | 卡牌 | `WavDecoder.cs`、`NativeWavPlayer.cs` | **作者本人** |
| 2.5.5 | 主界面按钮与横幅清理、坏 json 隔离、收藏刷新兜底 | 界面 / 内部 | `HomeMenuPatches.cs`、`ApplyCardMasterPatches` | 新增 |

### 2.5.6（本次）

| 模块 | 功能 | 代码位置 |
| --- | --- | --- |
| 卡牌 | **借特效（VFX + SE）**：`effectBorrowCardId` / `summonEffectCardId` / `destroyEffectCardId` / `evolveEffectCardId` / `attackEffectCardId` / `skillEffectCardId` | `ApplyBorrowedEffects`、`BorrowEffectFields`、`CopyEffectField`、`CollectBorrowedEffectResources` |
| 卡牌 | 特效包与 SE 自动加载 | `BorrowedEffectsByCardId`、`AddEffectBundle`、`AddSeBank`、`ResourcesManager_LoadAssetGroupAsync` 前缀 |
| 卡牌 | 卡面材质目录实测化（手牌紫红） | `RememberMaterialKindFromObjectPath`、`AssetManager.LoadObject` 补丁 |
| 卡牌 | 卡面目录兜底（field ↔ spell，含贴图/卡名横幅） | `CardFolderPairs`、`IndexOfCardFolder`、`FlipCardFolder`、`ResolveCardMaterialObjectPath` |
| 卡牌 | 借来的卡面包名纠正 | `AddArtSourceBundle`、`ResolveArtBundleName` |
| 卡牌 | 重印卡包名重定向（卡组编辑丢图） | `RedirectMissingCardBundles` |
| 卡牌 | 借来的资源组每次补拉 + 卡池预先请求 | `ResourcesManager_LoadAssetGroupAsync` 前缀、`RequestBorrowedArtFor` |
| 卡牌 | 材质 / 贴图保活（战斗中途变紫红、黑卡） | `ArtAssetsToKeepAlive`、`KeepArtAssetsAlive` |
| 卡牌 | 缺卡不再炸卡片列表 | `SortIDList_Prefix` |
| 卡牌 | 坏牌组不再炸牌组列表 | `CustomDeckStore.DropUnknownCards`、`CardExists` |
| 卡牌 | 热重载不再引用废弃的 `CardImages`/`CardVoices` | `DeckListHotReload.ComputeCardSignature` |
| 卡牌 | 剧情抉择卡（技巧/秘术/奥义）的 `Cost = -1` 日志越界 | `BattleLogPatch.cs` |
| 回放 | 离线 AI 战录像可用 | `ReplayOfflineData`（录像器接管、battle_id、`InitiateGameEndSequence`、`PracticeFinishTask`） |
| 回放 | 开局换牌两条 op 补齐 | `PlayerMulliganCtrl` / `OpponentMulliganCtrl` / `SingleMulliganMgr` 三个 postfix |
| 回放 | 职业与主战者补正 | `RepairReplayClassIds`、`ApplyBattleStartInfo`、`FindStoryBattleLeaders`、`LoadStoryBattleTable` |
| 回放 | 发牌空引用防护 | `UpdateSkillDescriptionValueList_Prefix` |
| 回放 | 「打开回放文件夹」按钮（随语言） | `ReplayDialog_SetupContents_Postfix`、`OpenReplayFolder`、`ResolveUiText` |
| 回放 | 播放诊断（临时） | `ReplayPlaybackTrace.cs` |
| 剧情 | 官方主战者表 / 别名 / 可用性、按篇章补人 | `EnsureStoryLeaderTable`、`GetOfficialStoryLeader`、`ResolveLeaderAlias`、`IsUsableLeader`、`EnsureLeaderCharaUsableTable` |
| 剧情 | 章节特殊战斗设置（`_chapter` 覆盖） | `CreateStoryStartResponse`、`ApplyChapterOverride`、`LogStoryBattleDiagnostics` |
| 剧情 | 章节表情表变体 | `StoryAiEmoteVariants.cs` |
| 剧情 | 词条 / 文本回退 | `StoryOfflinePatches`（`ParamMasterHandle.GetText`）、`StoryTextLanguagePatches` |
| 剧情 | 剧情对战 AI 接管 | `StoryBattleAiPatches.cs` |
| BossRush | 本地进度/存档、卡组与能力捕获、隐藏 Boss | `BossRushOfflineData`：`Initialize`、`GetState`、`CaptureDeck`、`CaptureAbility`、`CaptureFinish`、`CaptureHiddenFinish`、`ClearRun` |
| BossRush | 主战者选择窗口（按职业筛选、按 Boss 推荐、异步头像） | `BossRushLeaderSelectWindow.cs` |
| BossRush | 增益 / 礼包 / Boss 名简繁双语 | `ResolveLocalizedText`、`ResolveAbilityDescription`、`ResolveEnemySkillDescription`、`*_chs`/`*_cht` |
| BossRush | 章节一览与大厅界面清理、期间/结束时间移除 | `BossRushPatches`：`HideQuestPageMissionButtons`、`HideLocalBossRushButtonsAndDescriptions` |
| AI | 自定义练习入口按钮 | `AIManager.TryCreateBlueCustomPracticeButton`、`ResolveCustomPracticeGridPosition` |
| AI | 剧情 AI 选择对话框（官方牌组/风格/表情反查） | `CustomPracticeSetupWindow`：`LoadStoryAiChoices`、`OpenStoryAiDialog`、`ResolveStoryEnemyChara` |
| AI | 官方 AI 数据目录 | `PathHelper.OfficialAIDataPath`（`<资源根>/story_ai/{deck,style,emote}`） |
| AI | 出牌看门狗与兜底数据 | `AITurnGuard.cs`、`AICardDataFallback.cs`、`PracticeDualAI.cs`、`ActiveSkill.cs` |
| 离线 | 个人资料（等级/统计/皮肤/货币） | `ProfileOfflineData.cs` |
| 离线 | 解谜（分组表 + 全解锁） | `PuzzleOfflineData.cs`、`<资源根>/puzzle/puzzle_info.json` |
| 离线 | 空响应任务与网络等待上限 | `FakeConnect`：`IsEmptyOfflineTask`、`CreateEmptyOfflineResponse`、`WaitUntilNetworkIdle` |
| 界面 | 「其他」页：删除账号变暗、货币修改入口、位置调整 | `MyPageOtherPatches.cs` |
| 界面 | 结算界面移除「任务」按钮 | `BattleResultUiPatches.cs` |
| 界面 | 语言切换只留中文 | `LanguageSelectionPatches.cs` |
| 界面 | 主界面按钮与横幅清理 | `HomeMenuPatches.cs`（含解谜入口 `PracticePuzzleUI.onFirstStart`） |
| 内部 | 路径重定向 | `PersistentDataPathRedirect.cs`、`ResourceRootPatches.cs`、`PlayerPrefsRedirect.cs` |
| 内部 | 卡死修复与诊断 | `FakeConnect.ProcessOnlineTask`（try/finally）、`TaskWatchdog.cs`、`PerfTrace.cs` |
| 内部 | 配置项：`[Resources] Root` / `AllowTemporaryVoiceDownload` / `[Replay] RecordOfflineBattles` | `Plugin.cs` |
| 内部 | 构建：`Shadowverse.local.props` + `SHADOWVERSE_DIR`（仓库不写死机器路径） | `Shadowbus.csproj`、`.gitignore` |
| 工具 | 个人数据一键清理 | `D:\Games\Shadowbus\个人数据清理.bat` |
| 工具 | 引擎日志归位（`-logFile` / 目录联接） | `Shadowbus启动器.bat`、`Resources\游戏资源链接工具.bat` |
| 文档 | 卡牌 Mod 教程（含借特效、二代搬运两章） | `Mods/CardMaster/mod制作教程/mod卡教程.md` |
| 文档 | 网页版卡牌编辑器增强 | `WebEditor/`（`cardAssets.ts`、`game-root.mjs`、校验与测试） |

---

## 三、卡片系统

### 1. 卡牌资源目录：一张卡一个文件夹（2.5.5 起，2.5.6 收尾）

**问题**：一张自制卡要拆三个地方放——数据在 `CardMaster/`、卡图在 `CardImages/` 且必须叫 `<资源卡号>.png`、语音在 `CardVoices/` 下按另一套相对规则，三者靠卡号人工对齐，改名就失效，多张卡共用资源卡号还会互相覆盖。

**做法**：`Mods/CardMaster/` 下每个含 `.json` 的一级子文件夹就是一张卡，插件记录「资源卡号 → 卡文件夹」，卡图和语音都回各自文件夹里找。新增 `imageFiles` 声明卡图、`voiceFiles` 的路径改为相对卡文件夹且支持约定文件名（`play.wav` 等）。**2.5.6 把最后一处引用也去掉了**：`DeckListHotReload.ComputeCardSignature` 以前还把这两个目录算进热重载签名，现在只签名 `CardMaster` 与 `Format`，遗留的空目录可以直接删。

**结果**：一张卡的全部资源放一个文件夹，json 可以什么都不写；只放 json 也能加载。

### 2. 卡图

**问题**：借用原版卡面的卡在战场上被放大裁切（取景参数来自模板卡）；衍生卡（先谋 / 结晶 / 奥义）借不到卡面时整张全黑；卡面类型行空白；自制卡在牌组编辑里乱序。

**做法**：`InheritArtworkFraming` 把来源卡的 `NormalTilling` / `NormalOffset` / `EvolTilling` / `EvolOffset` 复制到本卡；`ResolveMissingArtwork` 在卡面找不到时跨卡种类（随从 ↔ 咒术魔法阵）兜底；补 `TribeName` 的自定义本地化回退；排序后置补丁覆盖 `UIBase_CardManager`、`CardBundleControllerBase`、`CardDestruct_CardBundleController` 三处列表来源。

**结果**：借来的图在场上和来源卡长得一样，衍生卡不再黑图，类型行有字，列表按费用排。

### 3. 借特效（2.5.6 新功能）

**问题**：自制卡想让技能「演出」和原版一样，只能手写 `SummonEffectPath` / `SkillEffectPath` / `SkillSe` 这些名字；而且写对名字也不一定有画面——引擎按名字去 `effect_<名字>.unity3d` 里取 `Effect/Effects/<名字>`，自制卡自己的包不存在、借来的名字又没人请求过，结果就是**静默无特效**（引擎取不到 GameObject，不报错、不崩）。

**做法**：新增 6 个声明式字段，和借卡图（`normalArtCardId`）、借语音库（`extraVoiceIds`）同一思路：

| 字段 | 借到的引擎字段 |
| --- | --- |
| `summonEffectCardId` | `SummonEffectPath` / `SummonSePath` / `SummonMoveType` / `SummonEffectType` / `SummonTime` |
| `destroyEffectCardId` | `DestroyEffectPath` |
| `evolveEffectCardId` | `EvolEffectPath` / `EvolSePath` / `EvoEffectType` / `EvolTime` |
| `attackEffectCardId` | `AtkEffectParameter`（特效/SE/移动方式/引擎类型/时长，普通 + 进化） |
| `skillEffectCardId` | `SkillEffectPath` / `SkillSe` / `SkillMoveType` / `SkillEffectEnginType` / `SkillEffectTime` / `SkillEffectTargetType` 与对应 `EvoSkill*` 六项 |
| `effectBorrowCardId` | 上面全部（各槽位比它更具体；json 自己写的字段又比借来的更优先） |

实现：`ApplyBorrowedEffects` → `BorrowEffectFields`（在 `PatchTemplate` **之前**调用，所以「明确写的赢过借来的」）→ `CopyEffectField` 逐属性抄；抄的是**来源卡的深拷贝**，不会和来源卡共享可变数据；闪卡版（自动派生）同样处理。`CollectBorrowedEffectResources` 把借来的特效名/SE 名登记下来，资源组加载时把 `effect_<名字>.unity3d` 补进清单（磁盘上没有的跳过），SE 的 `s/<se>.acb` 由引擎按卡面字段自行请求。

**结果**：json 里写一个来源卡号就能整套借用；教程第 7 章「借原版卡的特效」有完整字段表。

### 4. 卡面取图与材质（2.5.6 修的一批老问题）

游戏里卡面材质的对象路径只有 `Card/Field/Materials/` 和 `Card/Spell/Materials/` 两种，而卡的种类与图实际所在目录在官方数据里经常矛盾。2.5.6 把这条链上的坑一起修了：

1. **目录不再猜错**（手牌紫红的根因）。原来「实测目录」是在 `FindCardMaterial` 的后置补丁里按**调用方声明的 `type`** 记的，而 `FindCardMaterial` 对 ≥10 位的材质号**无视这个参数**、一律按 `Card/Field/Materials/` 拼路径 —— 于是明明躺在 field 的材质被记成 spell，纠正时就把**本来正确的请求改坏**。实测「肃清」系列抉择卡的 `1214410311_M` / `1215410311_M` 因此取不到材质，手牌**紫红、卡图丢失**（引擎自己的 `accumulate_log` 里正好是 `asset not found : Card/Spell/Materials/1214410311_M`）。现在目录只由**真正命中的那条对象路径**记录（`AssetManager.LoadObject` 的后置补丁 → `RememberMaterialKindFromObjectPath`，实测覆盖声明），并在 `AssetManager.LoadObject` 上加了**换目录兜底**（`ResolveCardMaterialObjectPath`：field ↔ spell 各试一次，命中就记下真实目录），材质 / 贴图 / 卡名横幅三对目录都覆盖。
2. **借来的包名算错**：`AddArtSourceBundle` 现在用 `ResolveArtBundleName` 纠正包名、磁盘上没有就不塞进加载清单（10 位材质号按 `<id>0.unity3d` 算出来的包名本地并不存在）。
3. **重印卡包名不存在**（卡组编辑丢图）。引擎给「重印 / 异画」卡算出来的包名（例如 `card_7203140210.unity3d`）在磁盘上不存在，真正装着卡面的是同组包 `card_7203140200.unity3d`；改名只发生在取材质那一步，**资源组加载清单里仍是不存在的名字** → 包从没被请求过 → 第一次取卡面就是空材质（丢图 / 黑卡），要滚动或刷新列表才恢复。新增 `RedirectMissingCardBundles`：加载清单里凡是 `card_<号>0.unity3d` 形式且磁盘上不存在的名字，换成真正存在的同组包。
4. **借来的包每次资源组都补**：以前只往**第一批**资源组里补（一次性开关），切界面时上一批被卸载后就再也不补了 → 现在每次资源组加载都补（已加载的包被引擎自己的 `QuickLoadAssetGroup` 跳过，不会重复加载）；卡池（卡组编辑 / 图鉴 / 分解）刚算出卡号时还会先 `RequestBorrowedArtFor` 把借来的包排进队列，赶在卡面渲染之前。
5. **材质保活**：游戏在进出界面时会调 `Resources.UnloadUnusedAssets`，没有任何托管引用的材质 / 贴图会被销毁 —— 借来的卡面交给引擎后插件自己不再引用，于是战斗中途卡面消失。现在 `KeepArtAssetsAlive` 把交出去的每一份材质与它的 `mainTexture` 记在静态列表里（静态字段引用算「在用」），回收扫不掉。

### 5. 本地卡牌语音

**问题**：本地语音全程不响，日志却显示一切正常。逐层查下来是三件事叠在一起——

1. `UnityWebRequestMultimedia` 在这些 wav 上返回「非 null 但 0 采样」的空 AudioClip，请求成功也不报错，播放代码以为加载完成；
2. 这游戏**关掉了 Unity 的音频子系统**（音频全由 CRIWARE/ADX2 原生输出，场景里连一个 AudioListener 都没有），`AudioClip.SetData` 直接失败并打印 `AudioClip contains no data`；
3. 闪卡是从模板的闪卡版本克隆的，没跟着普通卡走 `ApplyVoiceFiles`，所以照旧播模板语音。

**做法**：新增 `WavDecoder` 自己按 RIFF 解析 WAV（PCM 8/16/24/32、float32、`WAVE_FORMAT_EXTENSIBLE`），本地音频统一由 `NativeWavPlayer` 走 winmm 的 `PlaySound(SND_MEMORY | SND_ASYNC)` 直接输出，音量在样本上预缩放、静音不发声；闪卡的语音字段一并补上。既然 Unity 音频在工程设置里就是关的，Unity 那条路（`AudioSource`、`AudioClip`、`AudioListener`、`UnityWebRequest`）整个删掉了。因此本地语音**只支持 WAV**，MP3/OGG/AIFF 会明确提示改存 16-bit PCM。

**结果**：本地语音出声，音量与静音跟随游戏设置，`IsVoicePlaying` 也认得。

顺带确认过：`CriAtomExAcb.LoadAcbFile` / `CriAtomExPlayer.SetCue` 存在，理论上能加载作者自建的 ACB 走游戏原生音频；但 `CriAtomEx/Format` 只提供 `ADX` 与 `HCA`，**裸 WAV 喂不进 CRI**，那条路要求作者具备 CRI Atom Craft 工具链，所以没走。

### 6. 播放不再影响出牌动画

**问题**：原生播放会让出牌动画顿一下。不是没加载，是把两件贵活放在了播放那一帧——每次播放都克隆整段 wav 并逐样本缩放（244KB / 122253 次运算），加上 winmm 首次调用要初始化音频设备。

**做法**：缩放与内存固定在预加载阶段完成，播放时只剩一次 `PlaySound(指针)`；预加载时播 1 个样本的静音 WAV 预热 winmm。音量变了才重新缩放。

**结果**：播放不再卡动画，`Clear()` 会释放固定缓冲。

### 7. 缺卡 / 坏牌组不再炸整条流程（2.5.6）

**问题**：`UIBase_CardManager.SortIDList` 对每个卡号直接解引用：

```csharp
return (from id in idList
        let card = CardMaster.GetInstance(cardMasterId).GetCardParameterFromId(id)
        orderby new ComparableCard(card.CardId, cardMasterId)   // card 为 null 就 NullReferenceException
        select id).ToList();
```

实测触发场景：自制卡文件被删掉（例如删了 `Mods/CardMaster/皇棋/`）之后，旧牌组 `Mods/UnlimitedDecks/deck_9.json` 里还存着那 10 张的闪卡号 —— `GetCardParameterFromId` 返回 null，**整份本地牌组列表都注入不进去**（`DeckInfoTask` 报 NRE，牌组列表打不开）。

**做法**：两层。

- 牌组侧：`CustomDeckStore.LoadDeckList` 交给游戏前把「当前卡表里查不到」的卡号剔掉，并打一行点名日志（牌组号 / 名字 / 文件 / 具体卡号），牌组仍可打开编辑。
- 引擎侧兜底：`UIBase_CardManager.SortIDList` 加前缀，任何调用方（牌组码、导入、`FilterController`、服务器数据……）传进不存在的卡号都先滤掉；连卡表实例都拿不到时让列表原样通过（交给按费用排序的后置补丁），不再抛异常。

### 8. 数值、文本与参考表

- `intFields` / `floatFields` / `boolFields` / `intArrayFields` / `stringChangeFields` / `stringAppendFields` / `stringArrayFields` / `localizationFields` 的字段与语义保持 2.5.5 不变。
- `localizationFields.CardName` 改过名的卡会把新卡名补注册进 `BattleKeyWordDic`，描述里写这个卡名就是可点击的黄色词条（该字典每次进对局整本重建，补注册在重建之后再补一次）。
- 勇气抉择卡（技巧 / 秘术 / 奥义）在卡表里 `Cost = -1`：战斗日志只给费用 0..30 建桶，`_AddDestLogCommonOne` 拿 `Cost` 去桶里取会返回 null 再取 `.LogInfoList` —— 空引用，而且发生在出牌重试链上**每帧抛一次**，那张卡永远打不出去。`BattleLogPatch` 只在「这个费用没有桶」时跳过该行日志并打一行 `[BattleLog] card <id> has cost -1 …`，其余走原版（不伪造费用、不改卡表）。
- 卡表参考表导出（`CardSkillExporter`：`card_names.csv` / `card_skills.csv` / `card_resources.csv`）带耗时日志，供教程与排错查表。

---

## 四、回放（2.5.6 一条完整链路）

### 1. 离线录像可用

**问题**：游戏自带「新回放」机制（`NetworkBattleReplayOperationRecorder` 把一局录成 AES 加密的 JSON 放进 `<资源根>/NewReplay/<battleId>/`），但离线 AI 战拿的是 `NullReplayRecordManager`，什么都不写。

**做法**：换成真正的 `ReplayRecordManager`，并补上游戏自己缺的两处：

1. 开录前把双方开局牌组（`BattlePlayer/BattleEnemy.BattleStartDeckCardList`）塞进 `NetworkUserInfoData._selfDeck/_oppoDeck`（`RecordBattleStartInfo()` 就是从这里取牌组的）；
2. 真正写文件的 `BattleFinishWriteJsonData` 只挂在 `NetworkStandardBattleMgr.OnBattleFinish` 上，插件另外挂到 `AINetworkBattleManager.InitiateGameEndSequence(hasWon)` 与 `SingleBattleMgr.FinishBattle` 上。

离线局没有 battle_id：插件生成一个 12 位数字 id 当目录名（少于 14 位，不会被录像器自己的"清理没录完的时间戳目录"逻辑删掉）。`BattleManagerBase.JudgeBattleResult()` 每次出牌都会触发一次，所以只在「有一方主战者已经倒下」时才认它。

### 2. 播放：卡在换牌界面的两个根因

播放器靠 op 推进：`op 0` 发牌（`DealOperation`）、**第一个 `op 1`** 播我方换牌（`SwapOperation`）、**第二个 `op 1`** 收尾（`SecondMulliganOperation` → `OnEndMulligan`）。单机局这两条原版都不录：

1. `op 0`：原版只在 `NetworkPlayerMulliganCtrl` / `NetworkOpponentMulliganCtrl` 的 `StartMulliganVfx` 末尾调 `CallRecordingMulliganStart`，单机用的是它们的基类；
2. 敌方 `op 1`：`BattleEnemy.OnMulliganEndForReplay` 只在 `NetworkMulliganMgr.EnemyChangeCardVfx` 里发，单机的 `SingleMulliganMgr.EnemyChangeCardVfx` 只发 `CallRecordingMulligan`。

插件在这三处补发（只在「这一局正被我们录像」时动手）。**修好前录的录像缺这两条数据，仍然会停在换牌界面**（数据补不出来），重新录一局即可。

另外还有两处：

- **本地回放被判成服务器回放**：`GoReplay` 用 `Directory.GetDirectories(persistentDataPath + "/NewReplay").Select(d => d.Replace("\\","/")).FirstOrDefault(f => f == persistentDataPath + "/" + battleId)` 判定本地有没有这一局，而插件早期把资源目录（反斜杠）交出去 → 比较永远不成立 → 本地回放按服务器回放播，表现是**卡在换牌界面且日志没有任何报错**。现在 `PersistentDataPathRedirect.Current` 一律返回正斜杠（Unity 自己的风格），两边的判定重新对上。
- **剧情回放发牌那步的空引用**：`DealOperation → SetSkillDescriptionValueList(AllCards, CardInfoList)`，`GetBattleCardIdx` 找不到卡时返回 null，原版直接给 `card.ReplaySkillDescriptionValueList` 赋值就 NullReference，发牌流程整条断掉。插件只补「拿到 null 就跳过」。

### 3. 播放：职业与主战者补正

**问题**：离线录出来的 `replay_info.json` 里 `classId1/classId2` 是 **0**（录的时候 `DataMgr.GetPlayerClassId()` 与主战者卡的 `Clan` 都给 0），播放时 `OnReplayReady() → Matching.FirstSetting → StartBattleLoad → DataMgr.GetClassPrm(0)` 直接 `KeyNotFoundException`，对局起不来。而 `charaId1/2` 更难查：录像器写的是 `NetworkUserInfoData.GetSelfCharaId()/GetOpponentCharaId()`，它们读的**不是** `_selfInfo/_oppoInfo` 两个字典，而是 `SelfBattleStartInfo`/`OppoBattleStartInfo` —— 这两个对象只有「看录像恢复」那条路会建（`SetSelfInfo(info, isWatchReplayRecovery: true)`），离线对局一直是 null，取出来全是 0（同一局录出来的 `subclassId1=0` / `subclassId2=10` 就是证据）。播放时 `Matching.SettingOpponentClassDataAndLoadObject` 先按 `GetOpponentCharaId()`（=0 → null）再退 `GetCharaPrmByClassId(...)`，于是**敌方变成该职业的默认主战者**。

**做法**：两层都修。

1. **录的时候**：`ApplyBattleStartInfo` 按 `NetworkUserInfo.SetParameter` 要的键把 `SelfBattleStartInfo`/`OppoBattleStartInfo` 直接建出来（只反射写这两个属性，不碰 `DataMgr`，避免像原版 `SetNetworkSelfInfo` 那样顺带覆盖玩家职业/副职业/皮肤）。
2. **读老录像的时候**：职业非法就从牌组里数量最多的职业推；主战者号非法时，**剧情局先按 `fieldId` 去章节表反查**（回放里的 `fieldId` 就是 `Mods/StorySpecialBattles/<story>.json` 的 `battle3dfield_id`，官方在那里配了 `player_chara_id` / `enemy_chara_id`），按双方职业卡一遍、按「双方主战者」去重，分不清就不动、退回职业默认。**只对剧情局（`deck_format == DataMgr.BattleType.Story`）查**——练习局也能把战场选成剧情用过的背景（实测 `field=71/6/61/7`），光按 `fieldId` 查会把剧情主战者安到练习回放上。实测例：`fieldId=76` + 双方职业 8 → 唯一一章 → `player=1908, enemy=4528`。
3. 对**这一版之后**录的剧情局同样有效：录制那一刻 `DataMgr` 也拿不到剧情敌方的皮肤号（新录像里是 `enemy(class=8, chara=8)`），所以「等于该职业默认主战者」也当作待补，用章节表的值覆盖；只有录到的是别的皮肤时才尊重录像里的值。

### 4. 回放列表弹窗：「打开回放文件夹」

回放列表弹窗是 `ReplayDialog.Create()` 用 `DialogBase.SetButtonLayout(CloseBtn)` 建的那个（标题 `OtherTop_0033`），原版只带一个「关闭」。插件在 `ReplayDialog.SetupContents` 的后置补丁里往引擎自己的 `button2` 槽位加一个按钮（`ButtonType.Gray`，和「关闭」同一个 `btn_common_01_m_off` 贴图、同一套字号），并把两个槽位的锚点对调（引擎的两按钮布局是 button1 在右、button2 在左），让**「关闭」在左、新按钮在它右侧**；点击用 `explorer.exe` 打开录像目录且**不关弹窗**（`isNotCloseWindowButton2 = true`）。按钮名字跟着游戏的文字语言走：简中「打开回放文件夹」/ 繁中「開啟回放資料夾」/ 其它语言「Open Replay Folder」。路径不写死：`ReplayOfflineData.ReplayRoot` = 运行时解析出来的资源根 + `/NewReplay`。

### 5. 回放播放诊断（临时）

`ReplayPlaybackTrace.cs` 在播放时打 `[ReplayTrace]`：播放器实际消费的每条 op（开头 40 条 + 所有开局 op，带 type / isSelf / 两张下标表长度）、换牌收尾与阶段拆除、`StartBattle`、谁把回放置成暂停（带调用栈）、以及被静默吞掉的 op 异常。定位完可以整文件删掉（目前留着，方便以后排查卡住）。

---

## 五、剧情

### 1. 离线章节数据

`StoryOfflineData` 从本地资源清单拼章节表（实测 799 章，按 section 分组），补章节背景控制器，并按 `Mods/StorySpecialBattles/<story_id>.json` 的 `_chapter` 段修正每章的敌我配置（敌我主战者、职业、敌方 AI、战场、BGM、表情变体）。`_chapter` 只用于修正，**服务端正文缺失时必须返回空对象**（否则客户端会直接索引 `player_first_turn` 等键抛 `KeyNotFoundException`，这一章进不去战斗）。

### 2. 剧情主战者

官方 `main_story/info` 里的章节主战者 / 别名 / 可用性表被导出成本地表，离线时：

- `EnsureStoryLeaderTable` / `GetOfficialStoryLeader` / `GetSectionLeaderQuota` / `GetSectionProtagonistSkins` 决定「这一章该用哪几位主战者」；
- `ResolveLeaderAlias` 处理官方别名（同一皮肤的不同编号）；
- `IsUsableLeader` / `EnsureLeaderCharaUsableTable` 只挑玩家真的能用的皮肤；
- 官方数据不全时按职业补人（`GetClanLeaderCandidate`）。

日志里每章会打一行 `Applied local chapter override for story …`（敌我主战者、职业、AI、战场、BGM、表情表），核对不用猜。

### 3. 剧情战斗 AI 与表情

`StoryBattleAiPatches` 接管剧情 AI：本地 `Mods/AIData` 的牌组 / 风格 / 表情表 + `_ai` 段可完全接管某一章的出牌逻辑；`StoryAiEmoteVariants` 处理章节表情表变体（官方把表情按「皮肤_变体」拆成多张 CSV，变体表里的 ST 台词后缀带篇章号，据此选表）。进入战斗时逐条打日志（牌组、风格、表情、逻辑等级、内外表情开关），出问题不用猜。

### 4. 剧情文本与语言

剧情文本包按当前文本语言（`Cute.CustomPreference.GetTextLanguage()`）改路径：先找该语言目录（`story_text/<lang>/`），没有再退回简体，都不在就交回游戏原路径。语言码就是目录名小写（`Chs` / `Cht` / `Eng` …）。`StoryTextLanguagePatches.IsTraditionalChinese()` 给 BossRush 那类本地文本判断简繁；2.5.6 新增的 `ResolveUiText(simplified, traditional, english)` 让插件自己加的界面字也按语言取（繁体设置取繁体、简体设置取简体、其它语言取英文、读不出来按简体）。`StoryOfflinePatches` 里还有一处 `ParamMasterHandle.GetText` 的回退，兜住词条文本缺失。

---

## 六、BossRush

`BossRushOfflineData` 用本地 `Mods/BossRush/bossrush.json` 生成整条任务链（`QuestInfoTask` / `BossRushLobbyInfoTask` / `QuestBossRush*` / `BossRush*`，由 `FakeConnect.ProcessBossRushTask` 分发），并维护本地的**进度与存档**：

- 入口：`Initialize`、`ReloadPackages`、`SelectPackage`、`GetState`、`GetLobbyBackgroundName`；
- 记录：`CaptureDeck`、`CaptureAbility`、`CaptureFinish`、`CaptureHiddenFinish`、`ClearRun`；
- 流程：`GetCurrentBoss`、`GetNextBattleBossIndex`、`ResolveAiPath`；
- 存档落在 `Mods/BossRush/State/<包>.json`（`default.json` 等）。

界面侧：

- **主战者选择窗口**（`BossRushLeaderSelectWindow`）：按职业筛选、按当前 Boss 推荐、原生 UI 构建、头像异步载入并防重入；
- **文本**：增益、对手增益、礼包名 / 标题 / 正文、Boss 名都补了简繁两版字段（`*_chs` / `*_cht`），取值走 `ResolveLocalizedText`（繁体设置优先繁体、简体设置优先简体、只有一版中文时用那版；PP/EP 之类的拉丁缩写保持原样）；
- **清理**：章节一览的「单人挑战任务」「确认报酬」按钮、大厅里本地的关卡 / 领奖按钮与说明、期间与结束时间显示都隐藏；入口保持原版的「礼包 `display_name` + 奖励行」写法不动。

---

## 七、AI 与练习

- **自定义练习入口**：`AIManager.TryCreateBlueCustomPracticeButton` / `ResolveCustomPracticeGridPosition` 在职业选择页加一个蓝色按钮，进去就是自定义练习设置窗口。
- **剧情 AI 选择**（`CustomPracticeSetupWindow`）：`LoadStoryAiChoices` / `OpenStoryAiDialog` / `SelectStoryAi` 可以挑「官方剧情 AI」来当对手；`ResolveStoryEnemyChara` / `ResolveCharaClassId` / `ReadDeckCardIds` 从官方 AI 的牌组 / 风格 / 表情 CSV 反查敌方主战者、职业与牌组，`FindOfficialCsv` 找官方数据，`SetLeaderPageButtonEnabled` / `UpdateStoryAiControls` 管按钮可用性。
- **官方 AI 数据目录**：`PathHelper.OfficialAIDataPath` = `<资源根>/story_ai/{deck,style,emote}`（`Mods/AIData` 留给玩家自己写模组，两者不混）。
- **AI 稳定性**：`AITurnGuard`（AI 出牌卡住时的看门狗）、`AICardDataFallback`（AI 卡表里没有的卡给一份兜底评分，AI 才会考虑打出）、`PracticeDualAI`（练习对战的双方 AI）、`ActiveSkill.cs`（自定义主动技能支持）。

---

## 八、离线数据与界面

- **个人资料**（`ProfileOfflineData`）：等级 / 经验 / 统计封顶、皮肤与头像、货币与各类入场券；`Mods/Profile.json` 是它的落盘位置，也是「货币修改」按钮的写入目标。
- **解谜**（`PuzzleOfflineData`）：题目本身在客户端 master 里（`master_puzzle_data` → `master_puzzle_battle_data`，213 题），服务端只负责**分组**和**通关状态**；插件只需一份 `<资源根>/puzzle/puzzle_info.json`（`_tools/build_puzzle_data.py` 生成），并把所有条目标成已通关 / 可玩。判断用**任务类型名**而不是类型本身，所以不用在编译期引用客户端的解谜类。
- **「其他」页**（`MyPageOtherPatches`）：排队码 / 咨询弹窗里的「删除账号」变暗不可点（每 0.1 秒重压一次）；「游戏指南」与「入场券兑换码一览」按位置重排；「道具获得履历」整个移除（它每帧入场动画会 `SetActive(true)`，必须从 `_enableOtherButtons` 里摘掉）；空出来的位置放一个克隆自「设定」的按钮**「货币修改」**，点开可以改金币 / 以太 / 各种入场券，改完存回 `Mods/Profile.json`。回放列表入口也在这条链路上。
- **结算界面**（`BattleResultUiPatches`）：删掉 `ButtonGrid` 里的 `MissionBtnObj`（**所有模式**的结算都会出现「任务」按钮，代码里从不隐藏它），用 `Destroy` 而不是 `SetActive(false)`，并让 `UIGrid` 重新排版。
- **语言切换**（`LanguageSelectionPatches`）：客户端语言表一共 8 项，这里在 `SwitchLanguage.CreateSwitchLanguageDialog` 执行期间把静态数组**临时**换成只含繁体中文 / 简体中文的版本，方法一返回就还原（那个数组别处还在用：字体、语音语言表都按 `LangType` 去里面找，直接改会让别的语言查不到）。
- **主界面清理**（`HomeMenuPatches`）：隐藏首页的礼物 / 任务 / 公会 / 通行证、活动框、主横幅（含左右切换与底部小圆点）、副横幅；卡组页的比赛精选牌组；商店的购买卡套 / 角色皮肤 / 道具 / 交换临时卡牌 / 预组卡牌（含介绍条与维护提示牌）；单人页的解谜（`PracticePuzzleUI.onFirstStart`）。保留商店「礼包 / 特供」与卡包入口。做法都是各页面 `Show` 的后置补丁关掉控件自身的 GameObject，不动逻辑。
- **我的页面**：背景选择与列表按本地 `Mods/MyPageBackground.json` 恢复（离线时不会再随机跳）。
- **自定义赛制与卡组码**：`Mods/Format/*.json` 定义赛制（卡组张数、同名卡上限、Token 上限、单卡限张），`CustomFormats` / `CustomFormatDefinition` / `CustomFormatDeckEditRules` / `DeckFormatUI` / `CustomRoomFormatUI` 负责解读与界面；`LocalDeckCode` / `LocalDeckCodeIntegration` 让卡组码在离线也能生成与导入。
- **卡组编辑热重载**：`DeckListHotReload` 按文件时间戳决定要不要重建卡表（`Reference/` 里的导出 CSV 不参与判定），改完 json 进一次卡组列表即热重载。

---

## 九、路径、日志与诊断

### 1. `persistentDataPath` 重定向

游戏里所有 `Application.persistentDataPath` 的取值（18 处调用）以及启动最早期就已经存进静态字段的路径，一律指向资源目录：卡图 / 语音 / 影片 / manifest、回放目录 `NewReplay` 与 Record、HTTP 下载缓存、统计日志、`NGUITools` 存档、游戏自带的 CardMaster 导出目录、首页特殊称号的 BGM 检查。资源根顺序：`[Resources] Root` 配置 → 环境变量 `SHADOWBUS_RESOURCES` → `<游戏目录>\..\Resources` → `<游戏目录>\Resources` → `<游戏目录>\Shadowbus\Resources`；`ResolveResourcePath` 会把拼出来的路径重新按平台分隔符归一化。**注意 `persistentDataPath` 必须保持 Unity 自己的正斜杠风格**（回放判定就是拿它做字符串比较的）。

### 2. `PlayerPrefs` 重定向

Windows 上 Unity 的 `PlayerPrefs` 存在 `HKCU\Software\Cygames\Shadowverse`，插件把所有 `PlayerPrefs` 读写重定向到 **`<资源根>/PlayerPrefs.txt`**。注意不能只给 `Wizard.PlayerPrefsWrapper` 打补丁——它太短，Mono 会把方法体内联进调用方，绕开补丁；插件连**调用它的地方**一起换掉（实测拦下 415 处）。注册表旧值不会丢：第一次读不到就回落到注册表读一次并记进本地文件（惰性搬家），之后只读不写。唯一还在注册表里的是 Unity 播放器原生层自己维护的几个键（`unity.player_session_count` / `unity.player_sessionid` 等）。

### 3. 引擎日志

`Player.log` / `Player-prev.log` 是 Unity 原生（C++ 层）写的，托管代码改不了路径，所以两条路都指向资源目录：`Shadowbus启动器.bat` 启动时加 `-logFile "<资源目录>\Player.log"`；系统目录联接（`Resources\游戏资源链接工具.bat` 选 1）把 `%USERPROFILE%\AppData\LocalLow\<公司>\<产品>` 整个联接到资源目录 —— 联接对游戏透明，**不管用启动器、Steam 还是直接双击 exe**，日志都落在资源目录里（每台机器做一次）。插件本身不再依赖任何本机路径。

### 4. 卡死与看门狗

**根因**：只有插件代码会把 `NetworkManager.isConnect` 置位；一旦有异常逃出 `ProcessOnlineTask`，就会跳过 `isConnect = false`，之后每个任务都卡在 `while (isConnect) yield return 0;` 上。现在 `ProcessOnlineTask` 整体 `try { … } finally { ClearLastRequestTask(); isConnect = false; }`，并在三个任务入口用 `WaitUntilNetworkIdle` 做**上限 3 秒**的等待；`FakeConnect` 还会为空响应任务生成一份空数据，避免流程空转。

### 5. 诊断工具

- `TaskWatchdog`：日志里能看到 `[Task] '<名字>' finished after Ns (next: …)` 与 `[Hang] task '<名字>' has been pending Ns (isConnect=…, isOp=…)`，卡住时第一眼就知道卡在哪个任务。
- `PerfTrace`：给可能吃时间的补丁包一层跑表，`Enter(name)` 把当前操作挂到 `CurrentOperation` 上（卡死时能报出「插件当时在做什么」），单次超过 200 ms 打一行 `[Perf]`。
- `ReplayPlaybackTrace`（临时，见回放一节）。

---

## 十、工具与文档

- **`_reverse/PatchCheck.exe`**：加载插件与 `Assembly-CSharp`，解析每个 `[HarmonyPatch]` 目标并校验注入的实参名与 `___字段` 是否存在（字段沿基类向上找）。Harmony 按参数名匹配，名字对不上整条补丁会**静默挂不上**，编译期发现不了。发布前跑一次，2.5.6 的结果是 **266 个补丁方法，0 处不匹配**（期间真实拦下过 `isPlayer`/`isSelf`、`giftButton`/`_giftButton` 两类错误）。
- **`个人数据清理.bat`**（放在游戏目录旁边）：把这份游戏目录里的个人数据与日志 / 开发产物收进游戏目录**外面**的备份夹（`_清理备份_<时间戳>`），不是直接删除、可逐项还原；会检查游戏是否在运行，自动找游戏目录（改名 / 换盘 / 换层级都行）。清单：`Mods/P2PIdentity.json`、`Resources/PlayerPrefs.txt`、`Mods/BossRush/State/`、`Resources/NewReplay/`、`steam_settings/configs.user.ini` 的 `account_steamid` 行（只删这一行）、`_reverse/`、`LocalLow/`、几个开发残留文件、`BepInEx/LogOutput*.log`、`BepInEx/cache/`、`Resources` 下的各类日志。**不动**：`Mods/Profile.json`（昵称 / 皮肤 / 货币）、`Mods/MyPageBackground.json`、`Mods/UnlimitedDecks`（卡组）、`Mods/CardMaster`（自制卡）、资源包与游戏本体。
- **`游戏资源链接工具.bat`**（放在资源目录里）：把系统目录联接到资源目录，或还原成游戏默认目录；顺带在另一台机器上让引擎日志也落进资源目录。
- **`Mods/CardMaster/mod制作教程/`**：随游戏分发的卡牌 Mod 教程（第 7 章「借原版卡的特效」、第 10 章「从二代（Worlds Beyond）搬一张卡过来」），含字段速查表与排错表。
- **`WebEditor/`**：网页版卡牌编辑器（卡表编辑、资源槽位、校验、测试）；本版新增卡图资源模型 `cardAssets.ts`、游戏目录解析 `scripts/lib/game-root.mjs`，补了校验与测试用例。
- **构建与仓库**：`Shadowbus.csproj` 支持 `Shadowverse.local.props`（已被 `.gitignore` 忽略）或环境变量 `SHADOWVERSE_DIR` 指定游戏目录，**仓库里不写死任何用户名 / 机器专属路径**；`Plugin.cs` 的配置项新增 `[Resources] Root`、`[Resources] AllowTemporaryVoiceDownload`、`[Replay] RecordOfflineBattles`，并在启动日志里打构建时间横幅（判断日志是不是最新 DLL 产生的）。顺手删掉了 `BattleManagerPatcher.cs`——它只是 `BattleManagerBase.Update` 的一行 `LogMessage($"Update: {dt}")` 调试打印。

---

## 兼容性

| 项目 | 2.5.6 行为 |
| --- | --- |
| `Mods/CardImages/`、`Mods/CardVoices/` | 不再读取、不再创建，连热重载判定也不再引用（遗留空目录可直接删） |
| `Mods/CardMaster/*.json`（根目录） | 仍然加载；这类卡没有自己的文件夹，用不到本地卡图与本地语音 |
| `voiceFiles`、`imageFiles` | 字段名与子字段不变，含义仍是「相对卡自己的文件夹」 |
| 新增的借特效字段 | 全部是可选字段，不写就是旧行为（沿用模板卡自己的演出） |
| `CardMaster` 其它字段 | 全部不变，旧配置无需修改 |
| 回放 | 2.5.6 之前的离线录像：换牌两条 op 缺失的仍会停在换牌界面；`charaId=0` 的剧情录像现在能按章节表补出正确主战者 |
| 日志位置 | 引擎日志用启动器 `-logFile` 或目录联接归位（每台机器一次）；插件自身不依赖本机路径 |
| 迁移 | 资源目录里已包含设置、回放、剧情文本、自制卡；把 `Resources` 与 `Shadowverse` 一起复制即可 |

## 验证

- 构建：Release / net46，0 错误（`dotnet build Shadowbus.csproj -c Release`）
- 补丁校验：`_reverse/PatchCheck.exe` → **266 个补丁方法，0 处不匹配**
- 产出：`BepInEx/plugins/Shadowbus.dll`
- 现场核对过的几处：
  - 手牌紫红：`1214410311_M` / `1215410311_M` 只存在于 `card/field/materials/`，修正后引擎的 `asset not found` 消失；
  - 剧情回放主战者：`fieldId=76` → `player=1908` / `enemy=4528`；
  - 卡组列表 NRE：`deck_9` 引用已删除的 9 个卡号 → 现在只记一行警告，列表照常打开；
  - 卡组编辑丢图：`card_7203140210.unity3d` 不存在 → 重定向到 `card_7203140200.unity3d`；
  - 日志归位：`fsutil reparsepoint` 显示 Mount Point，用探针文件双向写入验证；
  - 个人数据清理：在临时副本上跑 `/y`，17 项如期收进备份夹、该留的一个没少、`configs.user.ini` 只掉 `account_steamid` 一行。
